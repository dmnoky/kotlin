## UNIT_10

## Переписать 8 юнит с сервлетами.
- Сервлеты в папке controller.
- Ещё улучшил тесты (в частности класс TestMain):
   - добавил контейнер постгреса для тестов - postgres: PostgreSQLContainer<*> = PostgreSQLContainer(DockerImageName.parse("postgres:latest"))
   - добавил ClassOrderer - теперь аннотация Order на классах учитывается в порядке запуска тестовых классов
   - но тест классы жоско связаны в очередности выполнения (заполенние тест-бд происходит через них). Сам себя осуждаю, но так захотелось.